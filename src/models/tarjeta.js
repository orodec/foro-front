export class tarjeta{
    title='';
    description='';
    question=0;

    constructor(title, description, question){
        this.title = title;
        this.description = description;
        this.question = question;
    }
}